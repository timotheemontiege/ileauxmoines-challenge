{
  "name": "tour-ile-challenge-backend",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "API REST + détection multi-parcours (winding number / waypoints) pour Tour Île Challenge — records de tours du Golfe du Morbihan.",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "backfill:vmax": "node scripts/backfill-vmax.js",
    "backfill:vmax:dry": "node scripts/backfill-vmax.js --dry-run"
  },
  "engines": {
    "node": ">=20.10"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "fast-xml-parser": "^4.5.0",
    "multer": "^2.0.1",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "vitest": "^2.1.1"
  }
}
